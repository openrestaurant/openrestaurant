{#
/**
 * @file
 * Template for Views Events: Events.
 */
#}
{% if title %}
  <h3>{{ title }}</h3>
{% endif %}
<div class="row">
  {% for row in rows %}
    {%
    set row_classes = [
    default_row_class ? 'view__row col-sm-4 margin--md--bottom',
    ]
    %}
    <div{{ row.attributes.addClass(row_classes) }}>
      {{ row.content }}
    </div>
  {% endfor %}
</div>
